services:
  - type: worker
    name: morning-short-paper-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: MODE
        value: PAPER
      - key: PAPER_SEED_USDT
        value: "1000"
